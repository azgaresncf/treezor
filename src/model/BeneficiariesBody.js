/*
 * Treezor
 * Treezor API.  more info [here](https://www.treezor.com). 
 *
 * OpenAPI spec version: 0.1.1
 *
 * NOTE: This class is auto generated by the swagger code generator program.
 * https://github.com/swagger-api/swagger-codegen.git
 *
 * Swagger Codegen version: 3.0.26
 *
 * Do not edit the class manually.
 *
 */
import {ApiClient} from '../ApiClient';
import {BeneficiariesSddB2bWhitelist} from './BeneficiariesSddB2bWhitelist';

/**
 * The BeneficiariesBody model module.
 * @module model/BeneficiariesBody
 * @version 0.1.1
 */
export class BeneficiariesBody {
  /**
   * Constructs a new <code>BeneficiariesBody</code>.
   * @alias module:model/BeneficiariesBody
   * @class
   * @param userId {Number} Beneficiary's userId
   * @param name {String} Beneficiary name, linked to bank account.
   */
  constructor(userId, name) {
    this.userId = userId;
    this.name = name;
  }

  /**
   * Constructs a <code>BeneficiariesBody</code> from a plain JavaScript object, optionally creating a new instance.
   * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
   * @param {Object} data The plain JavaScript object bearing properties of interest.
   * @param {module:model/BeneficiariesBody} obj Optional instance to populate.
   * @return {module:model/BeneficiariesBody} The populated <code>BeneficiariesBody</code> instance.
   */
  static constructFromObject(data, obj) {
    if (data) {
      obj = obj || new BeneficiariesBody();
      if (data.hasOwnProperty('tag'))
        obj.tag = ApiClient.convertToType(data['tag'], 'String');
      if (data.hasOwnProperty('userId'))
        obj.userId = ApiClient.convertToType(data['userId'], 'Number');
      if (data.hasOwnProperty('nickName'))
        obj.nickName = ApiClient.convertToType(data['nickName'], 'String');
      if (data.hasOwnProperty('name'))
        obj.name = ApiClient.convertToType(data['name'], 'String');
      if (data.hasOwnProperty('address'))
        obj.address = ApiClient.convertToType(data['address'], 'String');
      if (data.hasOwnProperty('iban'))
        obj.iban = ApiClient.convertToType(data['iban'], 'String');
      if (data.hasOwnProperty('bic'))
        obj.bic = ApiClient.convertToType(data['bic'], 'String');
      if (data.hasOwnProperty('sepaCreditorIdentifier'))
        obj.sepaCreditorIdentifier = ApiClient.convertToType(data['sepaCreditorIdentifier'], 'String');
      if (data.hasOwnProperty('sddB2bWhitelist'))
        obj.sddB2bWhitelist = ApiClient.convertToType(data['sddB2bWhitelist'], [BeneficiariesSddB2bWhitelist]);
      if (data.hasOwnProperty('sddCoreBlacklist'))
        obj.sddCoreBlacklist = ApiClient.convertToType(data['sddCoreBlacklist'], ['String']);
      if (data.hasOwnProperty('usableForSct'))
        obj.usableForSct = ApiClient.convertToType(data['usableForSct'], 'Boolean');
      if (data.hasOwnProperty('fields'))
        obj.fields = ApiClient.convertToType(data['fields'], ['String']);
    }
    return obj;
  }
}

/**
 * Custom data that could be used by caller to search the instance.
 * @member {String} tag
 */
BeneficiariesBody.prototype.tag = undefined;

/**
 * Beneficiary's userId
 * @member {Number} userId
 */
BeneficiariesBody.prototype.userId = undefined;

/**
 * Name choosen by end user to easily recognize the beneficiary.
 * @member {String} nickName
 */
BeneficiariesBody.prototype.nickName = undefined;

/**
 * Beneficiary name, linked to bank account.
 * @member {String} name
 */
BeneficiariesBody.prototype.name = undefined;

/**
 * Beneficiary address, linked to bank account.
 * @member {String} address
 */
BeneficiariesBody.prototype.address = undefined;

/**
 * Beneficiary International Bank Account Number. Mandatory if usableForSct is true.
 * @member {String} iban
 */
BeneficiariesBody.prototype.iban = undefined;

/**
 * Beneficiary Bank Identifier Code. Mandatory if usableForSct is true.
 * @member {String} bic
 */
BeneficiariesBody.prototype.bic = undefined;

/**
 * Beneficiary SEPA Creditor Identifier. Mandatory to validate incoming direct debit, useless other wise. Between 8 and 35 caracters. If sddB2bWhitelist or sddCoreBlacklist is provided then sepaCreditorIdentifier is mandatory.
 * @member {String} sepaCreditorIdentifier
 */
BeneficiariesBody.prototype.sepaCreditorIdentifier = undefined;

/**
 * Each unique mandate reference, with its frequency type, must be explicitely allowed when doing B2B Direct Debit. Furthermore, a mandate not used during more than 36 months will be automatically rejected even if in the white list. If provided, sepaCreditorIdentifier is mandatory.
 * @member {Array.<module:model/BeneficiariesSddB2bWhitelist>} sddB2bWhitelist
 */
BeneficiariesBody.prototype.sddB2bWhitelist = undefined;

/**
 * Core Direct Debit are accepted by default. If a Core mandate is to be refused on reception, it has to be added to this list. If wild char * (star) is used instead of a UMR, all Direct Debit from this beneficiary will be refused. If provided, sepaCreditorIdentifier is mandatory.
 * @member {Array.<String>} sddCoreBlacklist
 */
BeneficiariesBody.prototype.sddCoreBlacklist = undefined;

/**
 * Indicated if the beneficiary can be used for SEPA Credit Transfer. This field is a conveniant way to filter all beneficiaries for a user that would like to do a Credit Transfer. Indeed, beneficaries are created automatically when receiving a Core Direct Debit and therefor by looking at the list you won't be able to idnetify which beneficary to use on SCT.
 * @member {Boolean} usableForSct
 * @default false
 */
BeneficiariesBody.prototype.usableForSct = false;

/**
 * List of the object's properties you want to pick up.
 * @member {Array.<String>} fields
 */
BeneficiariesBody.prototype.fields = undefined;

